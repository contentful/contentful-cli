# Contentful CLI - `config list` command

List all the configs stored in `~/.contentfulrc.json`

## Usage
```
contentful config list

Options:
  -h, --help  Show help                                                [boolean]
  --version   Show version number                                      [boolean]

Examples:
  contentful config list

```

### Example
```sh
contentful config list
```
